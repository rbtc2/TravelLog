---
globs: *.css,*.html
description: TravelLog Premium Design System - 2025 Modern UI Standards
---

# 🎨 TravelLog Premium Design System

## 📋 Design Philosophy
TravelLog follows 2025 premium design standards with clean, modern aesthetics, subtle shadows, and sophisticated color palettes. All design elements prioritize user experience, accessibility, and visual hierarchy.

## 🏗️ CSS Architecture Overview

### **Import Order** (in [main.css](mdc:styles/main.css))
```css
/* 1. Design tokens and base styles */
@import url('./base/variables.css');
@import url('./base/reset.css');
@import url('./base/typography.css');
@import url('./base/forms.css');
@import url('./base/buttons.css');
@import url('./base/messages.css');
@import url('./base/layout.css');

/* 2. Utility systems */
@import url('./utilities/animations.css');
@import url('./utilities/accessibility.css');
@import url('./utilities/dark-mode.css');
@import url('./utilities/responsive.css');
@import url('./utilities/navigation-utils.css');
@import url('./utilities/scroll-optimization.css');

/* 3. Component library */
@import url('./components/buttons.css');
@import url('./components/forms.css');
@import url('./components/modals.css');
@import url('./components/cards.css');
@import url('./components/navigation.css');
@import url('./components/country-selector.css');

/* 4. Layout systems */
@import url('./layouts/main-layout.css');
@import url('./layouts/tab-navigation.css');
@import url('./layouts/tab-content.css');

/* 5. Page-specific styles */
@import url('./pages/login.css');
@import url('./pages/home.css');
@import url('./pages/search.css');
@import url('./pages/add-log.css');
@import url('./pages/calendar.css');
@import url('./pages/my-logs.css');
@import url('./pages/log-detail.css');
@import url('./pages/settings.css');
@import url('./pages/travel-report.css');
@import url('./pages/error.css');
```

## 🎨 Premium Design Token System

### **2025 Color Palette** (in [variables.css](mdc:styles/base/variables.css))
```css
:root {
  /* === SURFACE COLORS === */
  --surface-primary: #FFFFFF;
  --surface-secondary: #F8F9FA;
  --surface-tertiary: #F3F4F6;
  --surface-elevated: #FFFFFF;
  
  /* === TEXT HIERARCHY === */
  --text-primary: #1F2937;
  --text-secondary: #374151;
  --text-tertiary: #6B7280;
  --text-quaternary: #9CA3AF;
  --text-inverse: #FFFFFF;
  
  /* === SUCCESS/PROGRESS COLORS === */
  --success-primary: #10B981;
  --success-secondary: #059669;
  --success-light: #34D399;
  --success-background: #ECFDF5;
  --success-border: #A7F3D0;
  
  /* === ACCENT COLORS === */
  --accent-primary: #3B82F6;
  --accent-secondary: #1D4ED8;
  --accent-light: #60A5FA;
  --accent-background: #EFF6FF;
  --accent-border: #BFDBFE;
  
  /* === NEUTRAL GRAYS === */
  --gray-50: #F9FAFB;
  --gray-100: #F3F4F6;
  --gray-200: #E5E7EB;
  --gray-300: #D1D5DB;
  --gray-400: #9CA3AF;
  --gray-500: #6B7280;
  --gray-600: #4B5563;
  --gray-700: #374151;
  --gray-800: #1F2937;
  --gray-900: #111827;
  
  /* === SEMANTIC COLORS === */
  --error-primary: #EF4444;
  --error-secondary: #DC2626;
  --error-light: #F87171;
  --error-background: #FEF2F2;
  --error-border: #FECACA;
  
  --warning-primary: #F59E0B;
  --warning-secondary: #D97706;
  --warning-light: #FBBF24;
  --warning-background: #FFFBEB;
  --warning-border: #FDE68A;
  
  /* === TYPOGRAPHY SCALE === */
  --font-xs: 12px;
  --font-sm: 14px;
  --font-base: 16px;
  --font-lg: 18px;
  --font-xl: 20px;
  --font-2xl: 24px;
  --font-3xl: 28px;
  --font-4xl: 32px;
  --font-5xl: 36px;
  
  /* Font weights */
  --font-light: 300;
  --font-normal: 400;
  --font-medium: 500;
  --font-semibold: 600;
  --font-bold: 700;
  --font-extrabold: 800;
  
  /* Line heights */
  --leading-tight: 1.25;
  --leading-snug: 1.375;
  --leading-normal: 1.5;
  --leading-relaxed: 1.625;
  --leading-loose: 2;
  
  /* Letter spacing */
  --tracking-tighter: -0.05em;
  --tracking-tight: -0.025em;
  --tracking-normal: 0em;
  --tracking-wide: 0.025em;
  --tracking-wider: 0.05em;
  
  /* === SPACING SYSTEM === */
  --space-0: 0px;
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;
  --space-16: 64px;
  --space-20: 80px;
  --space-24: 96px;
  
  /* === BORDER RADIUS === */
  --radius-none: 0px;
  --radius-xs: 4px;
  --radius-sm: 6px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --radius-xl: 16px;
  --radius-2xl: 20px;
  --radius-3xl: 24px;
  --radius-full: 9999px;
  
  /* === PREMIUM SHADOW SYSTEM === */
  --shadow-none: 0 0 #0000;
  --shadow-xs: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --shadow-sm: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
  --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  --shadow-2xl: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
  --shadow-inner: inset 0 2px 4px 0 rgba(0, 0, 0, 0.06);
  
  /* Card shadows */
  --shadow-card-sm: 0 1px 3px rgba(0, 0, 0, 0.05);
  --shadow-card-md: 0 4px 12px rgba(0, 0, 0, 0.06);
  --shadow-card-lg: 0 8px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  --shadow-card-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  
  /* Interactive shadows */
  --shadow-hover: 0 10px 25px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
  --shadow-focus: 0 0 0 3px rgba(59, 130, 246, 0.1);
  --shadow-focus-error: 0 0 0 3px rgba(239, 68, 68, 0.1);
  --shadow-focus-success: 0 0 0 3px rgba(16, 185, 129, 0.1);
  
  /* === BORDER SYSTEM === */
  --border-width-0: 0px;
  --border-width-1: 1px;
  --border-width-2: 2px;
  --border-width-4: 4px;
  --border-width-8: 8px;
  
  /* Border colors */
  --border-subtle: 1px solid rgba(0, 0, 0, 0.06);
  --border-light: 1px solid var(--gray-200);
  --border-medium: 1px solid var(--gray-300);
  --border-strong: 1px solid var(--gray-400);
  --border-accent: 1px solid var(--accent-primary);
  --border-success: 1px solid var(--success-primary);
  --border-error: 1px solid var(--error-primary);
  
  /* === PREMIUM GRADIENTS === */
  --gradient-success: linear-gradient(90deg, #10B981 0%, #059669 100%);
  --gradient-accent: linear-gradient(90deg, #3B82F6 0%, #1D4ED8 100%);
  --gradient-surface: linear-gradient(180deg, #FFFFFF 0%, #F9FAFB 100%);
  --gradient-subtle: linear-gradient(135deg, #F8F9FA 0%, #E9ECEF 100%);
  
  /* === TRANSITIONS === */
  --transition-none: none;
  --transition-all: all 150ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-colors: color 150ms cubic-bezier(0.4, 0, 0.2, 1), background-color 150ms cubic-bezier(0.4, 0, 0.2, 1), border-color 150ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-opacity: opacity 150ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-shadow: box-shadow 150ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-transform: transform 150ms cubic-bezier(0.4, 0, 0.2, 1);
  
  /* === Z-INDEX SCALE === */
  --z-0: 0;
  --z-10: 10;
  --z-20: 20;
  --z-30: 30;
  --z-40: 40;
  --z-50: 50;
  --z-auto: auto;
}
```

## 📱 Responsive Design System

### **Mobile-First Breakpoints**
```css
/* Mobile: 320px - 767px (default) */
.component {
  padding: var(--space-4);
  font-size: var(--font-base);
}

/* Tablet: 768px - 1023px */
@media (min-width: 768px) {
  .component {
    padding: var(--space-6);
    font-size: var(--font-lg);
  }
}

/* Small Desktop: 1024px - 1279px */
@media (min-width: 1024px) {
  .component {
    padding: var(--space-8);
    font-size: var(--font-xl);
  }
}

/* Medium Desktop: 1280px - 1439px */
@media (min-width: 1280px) {
  .component {
    padding: var(--space-10);
    font-size: var(--font-2xl);
  }
}

/* Large Desktop: 1440px - 1599px */
@media (min-width: 1440px) {
  .component {
    padding: var(--space-12);
    font-size: var(--font-3xl);
  }
}

/* Extra Large Desktop: 1600px+ */
@media (min-width: 1600px) {
  .component {
    padding: var(--space-16);
    font-size: var(--font-4xl);
  }
}
```

### **Container System**
```css
:root {
  --container-mobile: 100%;
  --container-tablet: 90%;
  --container-desktop-sm: 1000px;
  --container-desktop-md: 1200px;
  --container-desktop-lg: 1400px;
  --container-desktop-xl: 1600px;
}

.container {
  width: var(--container-mobile);
  margin: 0 auto;
  padding: 0 var(--space-4);
}

@media (min-width: 768px) {
  .container { width: var(--container-tablet); }
}

@media (min-width: 1024px) {
  .container { width: var(--container-desktop-sm); }
}

@media (min-width: 1280px) {
  .container { width: var(--container-desktop-md); }
}

@media (min-width: 1440px) {
  .container { width: var(--container-desktop-lg); }
}

@media (min-width: 1600px) {
  .container { width: var(--container-desktop-xl); }
}
```

## 🎭 Premium Component Standards

### **Button Components** (in [buttons.css](mdc:styles/components/buttons.css))
```css
/* === PRIMARY ACTION BUTTON === */
.btn-primary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  padding: var(--space-3) var(--space-6);
  background: var(--accent-primary);
  color: var(--text-inverse);
  border: var(--border-width-1) solid transparent;
  border-radius: var(--radius-lg);
  font-size: var(--font-base);
  font-weight: var(--font-medium);
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-tight);
  cursor: pointer;
  transition: var(--transition-all);
  box-shadow: var(--shadow-sm);
  text-decoration: none;
  min-height: 44px; /* Touch target */
}

.btn-primary:hover:not(:disabled) {
  background: var(--accent-secondary);
  transform: translateY(-1px);
  box-shadow: var(--shadow-hover);
}

.btn-primary:active {
  transform: translateY(0);
  box-shadow: var(--shadow-sm);
}

.btn-primary:focus {
  outline: none;
  box-shadow: var(--shadow-focus);
}

.btn-primary:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none;
  box-shadow: var(--shadow-sm);
}

/* === SECONDARY BUTTON === */
.btn-secondary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  padding: var(--space-3) var(--space-6);
  background: var(--surface-primary);
  color: var(--text-primary);
  border: var(--border-medium);
  border-radius: var(--radius-lg);
  font-size: var(--font-base);
  font-weight: var(--font-medium);
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-tight);
  cursor: pointer;
  transition: var(--transition-all);
  text-decoration: none;
  min-height: 44px;
}

.btn-secondary:hover:not(:disabled) {
  background: var(--gray-50);
  border-color: var(--gray-400);
  transform: translateY(-1px);
  box-shadow: var(--shadow-sm);
}

/* === SUCCESS BUTTON === */
.btn-success {
  background: var(--success-primary);
  color: var(--text-inverse);
  border: var(--border-width-1) solid transparent;
}

.btn-success:hover:not(:disabled) {
  background: var(--success-secondary);
}

/* === DANGER BUTTON === */
.btn-danger {
  background: var(--error-primary);
  color: var(--text-inverse);
  border: var(--border-width-1) solid transparent;
}

.btn-danger:hover:not(:disabled) {
  background: var(--error-secondary);
}

/* === BUTTON SIZES === */
.btn-sm {
  padding: var(--space-2) var(--space-4);
  font-size: var(--font-sm);
  min-height: 36px;
}

.btn-lg {
  padding: var(--space-4) var(--space-8);
  font-size: var(--font-lg);
  min-height: 52px;
}

.btn-xl {
  padding: var(--space-5) var(--space-10);
  font-size: var(--font-xl);
  min-height: 60px;
}
```

### **Form Components** (in [forms.css](mdc:styles/components/forms.css))
```css
/* === FORM INPUT === */
.form-input {
  width: 100%;
  padding: var(--space-3) var(--space-4);
  background: var(--surface-primary);
  border: var(--border-medium);
  border-radius: var(--radius-lg);
  font-size: var(--font-base);
  font-weight: var(--font-normal);
  line-height: var(--leading-normal);
  color: var(--text-primary);
  transition: var(--transition-colors);
  min-height: 44px;
}

.form-input::placeholder {
  color: var(--text-quaternary);
  font-weight: var(--font-normal);
}

.form-input:focus {
  outline: none;
  border-color: var(--accent-primary);
  box-shadow: var(--shadow-focus);
}

.form-input:invalid {
  border-color: var(--error-primary);
  box-shadow: var(--shadow-focus-error);
}

.form-input:disabled {
  background: var(--gray-50);
  color: var(--text-quaternary);
  cursor: not-allowed;
}

/* === FORM LABEL === */
.form-label {
  display: block;
  font-size: var(--font-sm);
  font-weight: var(--font-medium);
  color: var(--text-secondary);
  margin-bottom: var(--space-2);
  line-height: var(--leading-tight);
}

.form-label--required::after {
  content: " *";
  color: var(--error-primary);
}

/* === FORM GROUP === */
.form-group {
  margin-bottom: var(--space-6);
}

.form-group--error .form-input {
  border-color: var(--error-primary);
  box-shadow: var(--shadow-focus-error);
}

.form-group--success .form-input {
  border-color: var(--success-primary);
  box-shadow: var(--shadow-focus-success);
}

/* === ERROR MESSAGE === */
.form-error {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  margin-top: var(--space-2);
  font-size: var(--font-sm);
  color: var(--error-primary);
  line-height: var(--leading-tight);
}

/* === SUCCESS MESSAGE === */
.form-success {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  margin-top: var(--space-2);
  font-size: var(--font-sm);
  color: var(--success-primary);
  line-height: var(--leading-tight);
}
```

### **Card Components** (in [cards.css](mdc:styles/components/cards.css))
```css
/* === PREMIUM CARD === */
.premium-card {
  background: var(--surface-primary);
  border: var(--border-subtle);
  border-radius: var(--radius-2xl);
  box-shadow: var(--shadow-card-lg);
  padding: var(--space-6);
  transition: var(--transition-all);
  position: relative;
  overflow: hidden;
}

.premium-card:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-hover);
  border-color: var(--gray-200);
}

.premium-card:focus-within {
  box-shadow: var(--shadow-focus);
}

/* === CARD HEADER === */
.card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space-4);
  padding-bottom: var(--space-4);
  border-bottom: var(--border-light);
}

.card-title {
  font-size: var(--font-xl);
  font-weight: var(--font-semibold);
  color: var(--text-primary);
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-tight);
  margin: 0;
}

.card-subtitle {
  font-size: var(--font-sm);
  font-weight: var(--font-normal);
  color: var(--text-tertiary);
  line-height: var(--leading-normal);
  margin: var(--space-1) 0 0 0;
}

/* === CARD CONTENT === */
.card-content {
  color: var(--text-secondary);
  line-height: var(--leading-relaxed);
}

.card-content p {
  margin: 0 0 var(--space-4) 0;
}

.card-content p:last-child {
  margin-bottom: 0;
}

/* === CARD FOOTER === */
.card-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: var(--space-6);
  padding-top: var(--space-4);
  border-top: var(--border-light);
}

/* === CARD VARIANTS === */
.card--elevated {
  box-shadow: var(--shadow-card-xl);
}

.card--flat {
  box-shadow: none;
  border: var(--border-light);
}

.card--bordered {
  border: var(--border-medium);
  box-shadow: none;
}

/* === STATS CARD === */
.stats-card {
  text-align: center;
  padding: var(--space-8);
}

.stats-value {
  font-size: var(--font-4xl);
  font-weight: var(--font-bold);
  color: var(--text-primary);
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-tighter);
  margin: 0;
}

.stats-label {
  font-size: var(--font-sm);
  font-weight: var(--font-medium);
  color: var(--text-tertiary);
  text-transform: uppercase;
  letter-spacing: var(--tracking-wide);
  margin: var(--space-2) 0 0 0;
}
```

### **Progress Components**
```css
/* === MODERN PROGRESS BAR === */
.progress-bar {
  width: 100%;
  height: 8px;
  background: var(--gray-200);
  border-radius: var(--radius-sm);
  overflow: hidden;
  position: relative;
}

.progress-fill {
  height: 100%;
  background: var(--gradient-success);
  border-radius: var(--radius-sm);
  transition: width 1.5s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
}

.progress-fill::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  animation: shimmer 2s infinite;
}

@keyframes shimmer {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(100%); }
}

/* === PROGRESS WITH LABEL === */
.progress-group {
  margin-bottom: var(--space-4);
}

.progress-label {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--space-2);
  font-size: var(--font-sm);
  font-weight: var(--font-medium);
  color: var(--text-secondary);
}

.progress-percentage {
  font-size: var(--font-sm);
  font-weight: var(--font-semibold);
  color: var(--success-primary);
}

/* === PROGRESS VARIANTS === */
.progress-bar--accent .progress-fill {
  background: var(--gradient-accent);
}

.progress-bar--large {
  height: 12px;
}

.progress-bar--small {
  height: 4px;
}
```

## 🌙 Dark Mode System

### **Dark Mode Variables** (in [dark-mode.css](mdc:styles/utilities/dark-mode.css))
```css
@media (prefers-color-scheme: dark) {
  :root {
    /* Surface colors */
    --surface-primary: #111827;
    --surface-secondary: #1F2937;
    --surface-tertiary: #374151;
    --surface-elevated: #1F2937;
    
    /* Text hierarchy */
    --text-primary: #F9FAFB;
    --text-secondary: #E5E7EB;
    --text-tertiary: #9CA3AF;
    --text-quaternary: #6B7280;
    
    /* Border colors */
    --border-subtle: 1px solid rgba(255, 255, 255, 0.1);
    --border-light: 1px solid #374151;
    --border-medium: 1px solid #4B5563;
    --border-strong: 1px solid #6B7280;
    
    /* Shadow adjustments */
    --shadow-card-sm: 0 1px 3px rgba(0, 0, 0, 0.3);
    --shadow-card-md: 0 4px 12px rgba(0, 0, 0, 0.4);
    --shadow-card-lg: 0 8px 25px -5px rgba(0, 0, 0, 0.5), 0 10px 10px -5px rgba(0, 0, 0, 0.2);
    --shadow-hover: 0 10px 25px -3px rgba(0, 0, 0, 0.3), 0 4px 6px -2px rgba(0, 0, 0, 0.2);
  }
}

/* Dark mode class override */
.dark {
  --surface-primary: #111827;
  --surface-secondary: #1F2937;
  --surface-tertiary: #374151;
  --surface-elevated: #1F2937;
  
  --text-primary: #F9FAFB;
  --text-secondary: #E5E7EB;
  --text-tertiary: #9CA3AF;
  --text-quaternary: #6B7280;
  
  --border-subtle: 1px solid rgba(255, 255, 255, 0.1);
  --border-light: 1px solid #374151;
  --border-medium: 1px solid #4B5563;
  --border-strong: 1px solid #6B7280;
  
  --shadow-card-sm: 0 1px 3px rgba(0, 0, 0, 0.3);
  --shadow-card-md: 0 4px 12px rgba(0, 0, 0, 0.4);
  --shadow-card-lg: 0 8px 25px -5px rgba(0, 0, 0, 0.5), 0 10px 10px -5px rgba(0, 0, 0, 0.2);
  --shadow-hover: 0 10px 25px -3px rgba(0, 0, 0, 0.3), 0 4px 6px -2px rgba(0, 0, 0, 0.2);
}
```

## ♿ Accessibility Standards

### **Accessibility Utilities** (in [accessibility.css](mdc:styles/utilities/accessibility.css))
```css
/* === SCREEN READER UTILITIES === */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

.sr-only-focusable:focus {
  position: static;
  width: auto;
  height: auto;
  padding: inherit;
  margin: inherit;
  overflow: visible;
  clip: auto;
  white-space: normal;
}

/* === FOCUS MANAGEMENT === */
.focus-visible:focus-visible {
  outline: 2px solid var(--accent-primary);
  outline-offset: 2px;
}

.focus-ring:focus {
  outline: none;
  box-shadow: var(--shadow-focus);
}

/* === HIGH CONTRAST MODE === */
@media (prefers-contrast: high) {
  :root {
    --text-primary: #000000;
    --text-secondary: #000000;
    --surface-primary: #FFFFFF;
    --border-medium: 2px solid #000000;
    --accent-primary: #0000FF;
  }
}

/* === REDUCED MOTION === */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}

/* === FOCUS TRAP === */
.focus-trap {
  position: relative;
}

.focus-trap::before,
.focus-trap::after {
  content: '';
  position: absolute;
  width: 1px;
  height: 1px;
  opacity: 0;
  pointer-events: none;
}
```

## 🎬 Premium Animation System

### **Animation Utilities** (in [animations.css](mdc:styles/utilities/animations.css))
```css
/* === FADE ANIMATIONS === */
@keyframes fadeIn {
  from { 
    opacity: 0; 
    transform: translateY(10px);
  }
  to { 
    opacity: 1; 
    transform: translateY(0);
  }
}

@keyframes fadeOut {
  from { 
    opacity: 1; 
    transform: translateY(0);
  }
  to { 
    opacity: 0; 
    transform: translateY(-10px);
  }
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes fadeInDown {
  from {
    opacity: 0;
    transform: translateY(-20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* === SCALE ANIMATIONS === */
@keyframes scaleIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

@keyframes scaleOut {
  from {
    opacity: 1;
    transform: scale(1);
  }
  to {
    opacity: 0;
    transform: scale(0.95);
  }
}

/* === SLIDE ANIMATIONS === */
@keyframes slideInLeft {
  from {
    opacity: 0;
    transform: translateX(-20px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes slideInRight {
  from {
    opacity: 0;
    transform: translateX(20px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

/* === ROTATE ANIMATIONS === */
@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

@keyframes bounce {
  0%, 100% {
    transform: translateY(-25%);
    animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
  }
  50% {
    transform: translateY(0);
    animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
  }
}

/* === ANIMATION UTILITY CLASSES === */
.animate-fade-in { 
  animation: fadeIn 0.3s cubic-bezier(0.4, 0, 0.2, 1); 
}

.animate-fade-out { 
  animation: fadeOut 0.3s cubic-bezier(0.4, 0, 0.2, 1); 
}

.animate-fade-in-up { 
  animation: fadeInUp 0.4s cubic-bezier(0.4, 0, 0.2, 1); 
}

.animate-fade-in-down { 
  animation: fadeInDown 0.4s cubic-bezier(0.4, 0, 0.2, 1); 
}

.animate-scale-in { 
  animation: scaleIn 0.2s cubic-bezier(0.4, 0, 0.2, 1); 
}

.animate-scale-out { 
  animation: scaleOut 0.2s cubic-bezier(0.4, 0, 0.2, 1); 
}

.animate-slide-in-left { 
  animation: slideInLeft 0.3s cubic-bezier(0.4, 0, 0.2, 1); 
}

.animate-slide-in-right { 
  animation: slideInRight 0.3s cubic-bezier(0.4, 0, 0.2, 1); 
}

.animate-spin { 
  animation: spin 1s linear infinite; 
}

.animate-pulse { 
  animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite; 
}

.animate-bounce { 
  animation: bounce 1s infinite; 
}

/* === ANIMATION DELAYS === */
.animate-delay-75 { animation-delay: 75ms; }
.animate-delay-100 { animation-delay: 100ms; }
.animate-delay-150 { animation-delay: 150ms; }
.animate-delay-200 { animation-delay: 200ms; }
.animate-delay-300 { animation-delay: 300ms; }
.animate-delay-500 { animation-delay: 500ms; }
.animate-delay-700 { animation-delay: 700ms; }
.animate-delay-1000 { animation-delay: 1000ms; }
```

## 🚫 **FORBIDDEN DESIGN PATTERNS**

### **❌ ABSOLUTELY PROHIBITED**
```markdown
❌ NEVER USE:
- Circular/donut progress bars (use horizontal only)
- 2000s-style buttons (bevel, emboss effects)
- Excessive drop shadows (--shadow-xl and above)
- Sharp corners (border-radius: 0)
- Center-aligned text (except headings)
- Web 2.0 style elements
- Purple/violet color schemes
- Overly bright gradients
- Neon colors
- Comic Sans or similar fonts
- 3D effects or transforms
- Blinking or flashing animations
- Auto-playing media
- Popup windows
- Fixed positioning abuse
```

### **✅ ALWAYS USE**
```markdown
✅ ALWAYS USE:
- Horizontal progress bars only
- Natural rounded corners (--radius-lg to --radius-2xl)
- Subtle shadows (--shadow-sm to --shadow-lg)
- Left-aligned text (except headings)
- Minimal design principles
- Modern color palettes
- Clean typography
- Smooth transitions
- Accessible contrast ratios
- Touch-friendly targets (44px minimum)
- Semantic HTML
- ARIA attributes
```

## 🎯 **Implementation Guidelines**

### **Priority 1: Remove Outdated Elements**
1. ❌ Delete all purple/violet color variables
2. ❌ Remove old gradient backgrounds
3. ❌ Eliminate excessive shadow values
4. ❌ Update outdated border-radius values

### **Priority 2: Apply Premium System**
1. ✅ Add new color palette
2. ✅ Implement modern shadow system
3. ✅ Build premium border system
4. ✅ Define sophisticated typography

### **Priority 3: Quality Validation**
1. ✅ Match screenshot color scheme
2. ✅ Follow modern design trends
3. ✅ Verify consistency and accessibility

## 🔧 **Naming Conventions**

### **BEM Methodology**
```css
/* Block */
.premium-card { }

/* Element */
.premium-card__header { }
.premium-card__content { }
.premium-card__footer { }

/* Modifier */
.premium-card--elevated { }
.premium-card--flat { }
.premium-card__header--large { }
```

### **Utility Classes**
```css
/* Display utilities */
.hidden { display: none; }
.block { display: block; }
.flex { display: flex; }
.grid { display: grid; }

/* Spacing utilities */
.p-0 { padding: var(--space-0); }
.p-1 { padding: var(--space-1); }
.p-2 { padding: var(--space-2); }
.p-3 { padding: var(--space-3); }
.p-4 { padding: var(--space-4); }
.p-6 { padding: var(--space-6); }
.p-8 { padding: var(--space-8); }

/* Text utilities */
.text-left { text-align: left; }
.text-center { text-align: center; }
.text-right { text-align: right; }

.text-xs { font-size: var(--font-xs); }
.text-sm { font-size: var(--font-sm); }
.text-base { font-size: var(--font-base); }
.text-lg { font-size: var(--font-lg); }
.text-xl { font-size: var(--font-xl); }

.font-light { font-weight: var(--font-light); }
.font-normal { font-weight: var(--font-normal); }
.font-medium { font-weight: var(--font-medium); }
.font-semibold { font-weight: var(--font-semibold); }
.font-bold { font-weight: var(--font-bold); }
```

## 🚀 **Best Practices**

### **✅ DO's**
- ✅ Always use CSS variables for consistency
- ✅ Follow mobile-first responsive design
- ✅ Use semantic HTML with proper ARIA attributes
- ✅ Implement proper focus indicators
- ✅ Support reduced motion preferences
- ✅ Use consistent naming conventions (BEM)
- ✅ Optimize for performance
- ✅ Test across different devices and browsers
- ✅ Maintain accessibility standards
- ✅ Use modern design patterns

### **❌ DON'Ts**
- ❌ Don't use hardcoded values (use CSS variables)
- ❌ Don't skip responsive design considerations
- ❌ Don't ignore accessibility requirements
- ❌ Don't create overly specific selectors
- ❌ Don't duplicate styles across files
- ❌ Don't use !important unless absolutely necessary
- ❌ Don't ignore browser compatibility
- ❌ Don't use outdated design patterns
- ❌ Don't create inaccessible interfaces
- ❌ Don't ignore performance implications

## 🔮 **Future Enhancements**

### **Phase 1: Design System Maturity**
- [ ] Expand component library
- [ ] Build design system documentation
- [ ] Implement automated style guide generation
- [ ] Add design token validation

### **Phase 2: Performance Excellence**
- [ ] Optimize CSS bundling and splitting
- [ ] Implement critical CSS extraction
- [ ] Add CSS performance monitoring
- [ ] Optimize animation performance

### **Phase 3: Developer Experience**
- [ ] Consider CSS-in-JS adoption
- [ ] Introduce style linting and validation
- [ ] Build automated visual regression testing
- [ ] Implement design system automation

---

**This design system ensures TravelLog maintains a premium, modern appearance that aligns with 2025 design standards while providing excellent user experience and accessibility.**